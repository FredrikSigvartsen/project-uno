
var value = $('#message_message').val();

if (value){
	// clear message box
	$('#message_message').val('');
	// show the message in the messages area
	$(document).ready(function(){
	      $('.messages').animate({
	        scrollTop: $('.messages')[0].scrollHeight}, 2000);
	});
}
